package arrays;

public class FirstNegativeNumberInEveryWIndowOfK {

    //given an array of numbers, find the 1st negative number in a subarray of k
    //have 2 pointers, left and right, left at 0, right at k
    //while left less equal to right
    //if num is less than 0 , append to result list
    //reset left to i , right to i + k
}
